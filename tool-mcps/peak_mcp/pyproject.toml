[project]
name = "peak-mcp"
version = "0.1.0"
description = "MRBIGR2 peak/haplotype visualization MCP — boxplots, haplotype tests, multi-trait Manhattan"
requires-python = ">=3.10"
dependencies = ["mrbigr2", "fastmcp>=2.0"]

[project.scripts]
peak-mcp = "server:main"
